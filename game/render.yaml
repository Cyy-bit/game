services:
  - type: web
    name: market-fog-investment-game
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    healthCheckPath: /health
    envVars:
      - key: HOST_PIN
        sync: false
